# Demo Accounts

All three demo accounts use the same password: **`password`**

| Role      | Name           | Email                        | Password  |
|-----------|----------------|------------------------------|-----------|
| Attendee  | Yassine Benali | `demo-user@example.com`       | `password` |
| Organizer | Salma Lahlou   | `demo-organizer@evently.test`| `password` |
| Admin     | Admin Evently  | `demo-admin@evently.test`    | `password` |

## Which account to use

- **Log in as attendee** (`demo-user@example.com`) for the public site, bookings, and tickets.
- **Log in as organizer** (`demo-organizer@evently.test`) for event management and door check-in.
- **Log in as admin** (`demo-admin@evently.test`) for the admin console (publish/reject/cancel events, tickets, payments).

## Where to log in

- Docker: http://localhost:8080/login
- Herd: http://evently.test/login